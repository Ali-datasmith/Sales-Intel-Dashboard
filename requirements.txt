# ============================================================================
# Sales Intel Terminal - Dependency Manifest (Python 3.14+ Optimized)
# ============================================================================
# NOTE: Version constraints changed to `>=` to support modern Python 3.14 
# runtimes on Streamlit Cloud while ensuring modern engine stability.
# ============================================================================

# ── Core Framework ──
streamlit>=1.36.0              # Web app framework + sidebar/session management
plotly>=5.22.0                 # Interactive charts (Funnel, Heatmap, Bar, Line)

# ── Data Processing ──
pandas>=2.2.0                  # Tabular data manipulation
polars>=1.0.0                  # High-speed columnar DataFrame engine (Rust-backed)
pyarrow>=14.0.2                # Polars ↔ Pandas interop + Excel parsing
duckdb>=1.0.0                  # In-memory SQL analytics on Polars DataFrames

# ── File I/O ──
openpyxl>=3.1.2                # Excel file reading/writing (supports modern python targets)

# ── Authentication & Security ──
bcrypt>=4.1.1                  # Password hashing for login.py credentials
python-dotenv>=1.0.0           # Optional: .env file management
